package Interface;

public class Interface_Main {

	public static void main(String[] args) {
	/*
	 * Interface is a collection of abstract methods.
	 * class can implement an interface.
	 * interface can extend from another interface.
	 * non abstract class can extending from an abstract class needs to be implement unimplemented methods.
	 * interface can not be instantiated.
	 * Interface does not have constructor.
	 * In addition to abstract methods interface can contains static and default methods.
	 * Interface can not have a data members. all variables by default consider as static and final.
	 * Everything is public by default.
	 * 
	 */
	}

}
